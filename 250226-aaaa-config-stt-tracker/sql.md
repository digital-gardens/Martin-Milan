

// I am a hobbyist

I have 2 tsv files that I want to
 combine into a single SQLite database

is it possible to do this using a tool, that runs inside chrome?

is it possible without writing anything like 

```
BEGIN TRANSACTION;
CREATE TABLE my_table (...);

```

NOTE.. i have a data schema in the tsv files, where it is completely OK to consider everything as TEXT
(VARCHAR), so I don't need to worry about data types.


is Datasette a good option for this?


--------------

so all 1..3 have sqlite in webassembly?
are they all free of paying?

how they survive then?
which ones are freemium
is any of them open source
do they have any github presence

any reddit voting??

dbxlite??
does it require me to write CREATE TABLE
-----------------

so maybe i should restart my research

so i need a sql database that can run inside chrome, and can import tsv files into a single table, without me having to write any statements 

 CREATE TABLE
  or
   BEGIN TRANSACTION
   
preferably
freemium


preferably with some github presence, or some reddit score


---------
sorry
I have to clarify
i am ok with writing a single CREATE TABLE name3 AS SELECT ...
I want to avoid writing CREATE TABLE my1_tsv (with types  of 30 named columns)



