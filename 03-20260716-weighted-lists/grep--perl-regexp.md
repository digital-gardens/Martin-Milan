
todo vscode regex flavor  ...  https://code.visualstudio.com/docs/editor/codebasics#_find-and-replace  ??

https://www.gnu.org/software/grep/manual/grep.html
in a UTF-8 locale grep and .git grep always treat data as UTF-8.

extension://hnncfhodpmpjchmmcnimoimkcojdmfhl/delayed.html?1&https://git-scm.com/docs/git-grep


-->


https://www.pcre.org/current/doc/html/pcre2syntax.html#SEC20


_---->


yy
/^[ ]*[-+*#][ ]+[#]{1,}[^#]/gmiux
m modifier: multiline. Causes ^ and $ to match the start and end of each line, not only the start and end of the string

g uppercase and lowercase letters
x modifier: extended. Ignores unescaped whitespace and comments outside character classes

u modifier: unicode. Treats the pattern and subject as UTF-16
https://regex101.com/

```

#q     auto{

-  ### ℹ️ #aaaaa *G*, ve 3/4 rytmu: 155 bpm 1. Máš, má .ovečko, dávno spát, už píseň ptáků končí Kvůli nám přestal vítr vát, jen můra ...


```


grep--perl-regexp.md
bash

grep --version
grep --help | head
git grep --version |head

yyyy
grep --help|grep --perl-regexp -i perl

yyyy
grep --help|   git grep --perl-regexp -i perl
q

yy
git grep --help|grep -i perl








https://www.gnu.org/software/grep/manual/grep.html

in a UTF-8 locale grep and .git grep always treat data as UTF-8.
yyyy

set | grep -i utf | clip
LANG=en_US.UTF-8

set | grep -i lc_

```grep output
 LANG=C LC_ALL=C git merge -s help 2>&1 | sed -n -e '/[Aa]vailable strategies are: /,/^$/{
```
